apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: desafio-app
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /v1
            pathType: Prefix
            backend:
              service:
                name: desafio-app
                port:
                  number: 80
